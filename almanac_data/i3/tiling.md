Tiling is a way of managing windows.

### Modern Desktops

Many modern desktops such as `Microsoft Windows` or `Mac OSX` have **floating window managers**. (There are also these on Linux such as `Gnome` or `XFCE`) 

Cool stuff aye? 

Nothing beats this.